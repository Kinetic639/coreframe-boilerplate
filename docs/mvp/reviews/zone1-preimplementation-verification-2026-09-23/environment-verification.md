# Zone 1 — Environment / Schema Verification

Fresh, live-verified today (2026-09-23) against `supabase-target` (`rjeraydumwechpjjzrus.supabase.co`, confirmed via `mcp__supabase-target__get_project_url`). `supabase-legacy` MCP tools returned a permission error in this session (`ProtocolError -32600`) — legacy-tree claims below rely on repo/file evidence, not a live legacy query.

## Runtime environment

`apps/web/.env.local`'s `NEXT_PUBLIC_SUPABASE_URL` = `https://rjeraydumwechpjjzrus.supabase.co` (the TARGET project) — this is what the running app actually reads. Live-verified matching via `get_project_url`.

**Stale instruction found**: `apps/web/CLAUDE.md` line 19 says _"NEVER run local Supabase. Always use remote project ID: zlcnlalwfmmtusigeuyk"_ — this is the **legacy** project ref, directly contradicting the live `.env.local` value and every recent Inventory Core migration. **Do not follow this CLAUDE.md instruction for project selection — it is wrong and should be corrected in a future documentation pass** (out of scope for this read-only verification itself).

## Authoritative migration tree: `apps/web/supabase-target/supabase/migrations`

Confirmed via 4 independent signals, not just inherited from prior docs:

1. Live DB is the target project (above).
2. `git log` recency: target tree's last commit is **today** (Inventory Core IC-8/A7/A8 work); legacy tree's last commit is **2026-07-11** (~2.5 months stale).
3. `docs/inventory/inventory-core-progress.md` cites target-tree paths exclusively for all applied migrations.
4. `docs/mvp/zones/01-auth-org-branch-access.md` (the already-accepted Zone 1 doc) explicitly states the same conclusion.

A third, byte-identical copy of the legacy tree exists at `apps/public-web/supabase/migrations` (274 files, identical filenames) — a duplicate, not an independent source.

## Schema gap table

| Table/Object                                            | Migration in target tree?                 | Live exists? | RLS         | FORCE?                 | `branch_id`?                            | Notes                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------- | ----------------------------------------- | ------------ | ----------- | ---------------------- | --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `organizations`                                         | Yes                                       | Yes          | true        | true                   | n/a                                     | No gap                                                                                                                                                                                                                                                                                                                                                                                    |
| `organization_members`                                  | Yes                                       | Yes          | true        | true                   | n/a                                     | No gap                                                                                                                                                                                                                                                                                                                                                                                    |
| `user_preferences`                                      | Yes                                       | Yes          | true        | true                   | n/a                                     | No gap — holds `organization_id`/`default_branch_id`                                                                                                                                                                                                                                                                                                                                      |
| `branches`                                              | Yes                                       | Yes          | true        | true                   | n/a                                     | No gap                                                                                                                                                                                                                                                                                                                                                                                    |
| `user_role_assignments`                                 | Yes                                       | Yes          | true        | true                   | n/a (scope via `scope_type`)            | No gap                                                                                                                                                                                                                                                                                                                                                                                    |
| `roles`                                                 | Yes                                       | Yes          | true        | true                   | n/a                                     | No gap — has `scope_type`, `is_basic`, immutability trigger                                                                                                                                                                                                                                                                                                                               |
| `permissions`                                           | Yes                                       | Yes          | true        | **false (deliberate)** | n/a                                     | No gap — migration's own comment says "Mirrors LEGACY (rls_forced = false)," intentional                                                                                                                                                                                                                                                                                                  |
| `role_permissions`                                      | Yes                                       | Yes          | true        | true                   | n/a                                     | No gap                                                                                                                                                                                                                                                                                                                                                                                    |
| `user_effective_permissions`                            | Yes                                       | Yes          | true        | true                   | **yes**                                 | No gap — confirmed as the real table read by `load-user-context.v2.ts:119`, `load-admin-context.v2.ts:24`                                                                                                                                                                                                                                                                                 |
| `invitations`                                           | Yes                                       | Yes          | true        | true                   | n/a                                     | No gap                                                                                                                                                                                                                                                                                                                                                                                    |
| `warehouse_locations`                                   | **NO** — only in legacy tree              | Yes          | true        | **true**               | **yes**                                 | **Gap.** Table itself is correctly FORCE-RLS'd and branch-scoped live — only the paper trail is wrong. Target's own `list_migrations` shows an applied entry `warehouse_locations_table` with **no corresponding file in either tree**.                                                                                                                                                   |
| `wdd_matcher_sessions`/`blocks`/`lines`/`session_files` | **NO** — only in legacy tree              | Yes (all 4)  | true        | **false (all 4)**      | sessions = yes; 3 detail tables = no    | **Gap, reconfirmed live today.** RLS enabled but NOT forced, org-scoped only despite a `branch_id` column on the session table.                                                                                                                                                                                                                                                           |
| `qr_codes` / `qr_assignments`                           | **NO — missing from BOTH trees entirely** | Yes (both)   | true (both) | false (both)           | `qr_assignments` = yes; `qr_codes` = no | **Worse than previously documented.** Target's `list_migrations` shows 3 applied entries (`qr_platform_foundation`, `qr_platform_permissions`, `qr_warehouse_locations_backfill`) whose filenames don't exist ANYWHERE in the repository — not even in legacy. Version/name timestamp mismatch suggests out-of-band application. **No reviewable source exists for these tables at all.** |
| `app_attachments`                                       | **NO** — only in legacy tree              | Yes          | **true**    | **true**               | no                                      | Gap — but live-correct (FORCE-RLS'd); only the source file is missing from the authoritative tree, and even the specific applied-migration filename doesn't exist in legacy either.                                                                                                                                                                                                       |
| `helpdesk_tickets`                                      | **NO** — only in legacy tree              | Yes          | true        | **false**              | **yes**                                 | Gap, reconfirmed live today — same organization-level-only RLS pattern as Matcher.                                                                                                                                                                                                                                                                                                        |

**Confirmed drift pattern**: every pitch-critical table missing from the target tree nonetheless appears as an APPLIED migration in `list_migrations`, under a filename matching legacy (4 tables) or matching nothing anywhere (QR). This means the live target schema was built partly by applying legacy-tree files (or wholly uncommitted files) directly against the target project, without ever copying them into the target tree. **Schema reconciliation (Zone 1's own decision 44) cannot be done by "picking the right tree" — some live objects (QR) have no committed source in any tree and would need to be reverse-engineered from `pg_catalog`/`information_schema` from scratch.**

## Answers to the 4 governing questions

1. **Authoritative target for new Zone 1 work**: `apps/web/supabase-target/supabase/migrations`, backing `rjeraydumwechpjjzrus`.
2. **Provable from repository code alone (target tree)**: `organizations`, `organization_members`, `user_preferences`, `branches`, `user_role_assignments`, `roles`, `permissions`, `role_permissions`, `user_effective_permissions`, `invitations` — all real, RLS-forced (except the one deliberate exception), reviewable.
3. **Must be proven from live schema only** (no target-tree migration): `warehouse_locations`, all 4 `wdd_matcher_*` tables, `app_attachments`, `helpdesk_tickets` (source exists in legacy only) — and `qr_codes`/`qr_assignments` (source exists nowhere).
4. **Pitch-safe technical debt vs. genuine blocker**:
   - **Pitch-safe technical debt** (runtime is correct, only the paper trail is wrong): `warehouse_locations`, `app_attachments` — both live FORCE-RLS'd and correctly scoped; missing only their committed source.
   - **Genuine gaps relevant beyond a single controlled demo** (pilot blockers, and a real demo-risk if the chosen scenario happens to touch them): `wdd_matcher_*` and `helpdesk_tickets` — RLS enabled but NOT forced, org-scoped only despite live `branch_id` columns; any org member can read/act on another branch's Matcher session or ticket. `qr_codes`/`qr_assignments` — no reviewable schema/RLS source anywhere, cannot be code-reviewed or regression-tested against a committed definition at all.
