# Zone 3 — Phase 10C (Container Orchestration) — Changed Files

Baseline: `620bf2001e9d2aa9dea4e6933f9e0283cb53c1b3` ("phase10b"), clean tree confirmed via `git status` before Phase 10C began, and reconfirmed clean (containing only Phase 10C's own changes) before this correction pass began.

**Includes the 2026-09-14 same-day external-review correction pass** (location-identity + RepairOrder-ownership invariants on `inventory_add_to_container`, a raw-write RLS boundary on `inventory_containers`/`inventory_container_lines`, and a genuine two-connection concurrency proof).

18 files changed, **2885 insertions(+), 31 deletions(-)** (`git diff --stat`, cross-validated against `zone3-phase10c-container.diff`: 3129 diff lines / 218285 bytes).

## Application code (5 files, 1369 insertions)

| File                                                                   |  +/- | What changed                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------------------- | ---: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/src/server/services/repair-orders.service.ts`                | +474 | New `REPAIR_ORDER_CONTAINER_KNOWN_ERRORS` allowlist (17 entries, including 2 added in the correction pass) + `normalizeContainerRpcError()`; 3 new result types; new `resolveRepairOrderScope()` helper; 3 new public methods (`createContainerForRepairOrder`, `placeAllocationInContainer`, `removeAllocationFromContainer`). |
| `apps/web/src/server/services/__tests__/repair-orders.service.test.ts` | +684 | New `buildContainerSupabaseMock()` helper + container fixtures; 18 new tests across 3 `describe` blocks (5 create-container, 8 place-allocation including 2 new known-error-passthrough tests from the correction pass, 5 remove-allocation).                                                                                   |
| `apps/web/src/app/actions/workshop/repair-orders.ts`                   | +106 | 3 new server actions (`createRepairOrderContainerAction`, `placeAllocationInContainerAction`, `removeAllocationFromContainerAction`), all gated on `WAREHOUSE_INVENTORY_OPERATE`.                                                                                                                                               |
| `apps/web/src/server/audit/event-registry.ts`                          |  +70 | 3 new Mode-A events: `workshop.repair_orders.container_created`, `workshop.repair_orders.stock_placed_in_container`, `workshop.repair_orders.stock_removed_from_container`.                                                                                                                                                     |
| `apps/web/src/lib/validations/repair-orders.ts`                        |  +35 | 3 new zod schemas: `createRepairOrderContainerSchema`, `placeAllocationInContainerSchema`, `removeAllocationFromContainerSchema` + inferred types.                                                                                                                                                                              |

## Database (11 files, 1163 insertions)

| File                                                                                                                                     |            +/- | What it does                                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------------------------------------- | -------------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/supabase/tests/100_repair_order_container_orchestration_phase10c_test.sql`                                                     |     +692 (new) | 44 live pgTAP assertions (was 29 before the correction pass) — see `migration-summary.md` and `review-context.md` for the full scenario list.                                |
| `apps/web/supabase-target/supabase/migrations/20260914131300_inventory_containers_empty_status.sql`                                      |       +3 (new) | Additive `'empty'` value on `inventory_containers.status` CHECK.                                                                                                             |
| `apps/web/supabase-target/supabase/migrations/20260914131308_inventory_alloc_container_lines_org_branch_uidx.sql`                        |       +2 (new) | Composite unique indexes on `inventory_allocation_lines`/`inventory_container_lines`.                                                                                        |
| `apps/web/supabase-target/supabase/migrations/20260914131318_inventory_allocation_container_links_table.sql`                             |      +36 (new) | New `inventory_allocation_container_links` table + composite FKs + RLS.                                                                                                      |
| `apps/web/supabase-target/supabase/migrations/20260914131333_inventory_create_container_rpc.sql`                                         |      +62 (new) | `inventory_create_container` RPC.                                                                                                                                            |
| `apps/web/supabase-target/supabase/migrations/20260914131352_inventory_add_to_container_rpc.sql`                                         |     +129 (new) | `inventory_add_to_container` RPC, first version (superseded twice — see below).                                                                                              |
| `apps/web/supabase-target/supabase/migrations/20260914131405_inventory_remove_from_container_rpc.sql`                                    |     +122 (new) | `inventory_remove_from_container` RPC, first version (superseded once).                                                                                                      |
| `apps/web/supabase-target/supabase/migrations/20260914131412_inventory_seal_container_rpc.sql`                                           |      +47 (new) | `inventory_seal_container` RPC.                                                                                                                                              |
| `apps/web/supabase-target/supabase/migrations/20260914131532_inventory_remove_from_container_rpc_fix_check_violation.sql`                |     +129 (new) | `CREATE OR REPLACE` fix for the full-removal `CHECK(quantity > 0)` violation.                                                                                                |
| **`apps/web/supabase-target/supabase/migrations/20260914192552_inventory_add_to_container_location_and_repair_order_ownership_fix.sql`** | **+160 (new)** | **Correction pass**: `CREATE OR REPLACE` adds the allocation/container location-identity check and the RepairOrder-ownership identity check to `inventory_add_to_container`. |
| **`apps/web/supabase-target/supabase/migrations/20260914192605_inventory_containers_repair_order_write_boundary.sql`**                   |  **+73 (new)** | **Correction pass**: 6 new `RESTRICTIVE` RLS policies closing the raw-write bypass on `inventory_containers`/`inventory_container_lines` for RepairOrder-owned rows only.    |

## Documentation (2 files, 92 insertions / 31 deletions)

| File                                                     |     +/- | What changed                                                                                                                                                                                                                |
| -------------------------------------------------------- | ------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/mvp/zones/03-repair-orders-implementation-plan.md` | +74/-31 | Phase 10C section rewritten with live-verified findings (first pass) + a new "Correction pass" subsection appended (this pass), documenting all four fixes explicitly rather than silently rewriting the original findings. |
| `docs/mvp/zones/03-repair-orders-progress.md`            |  +18/-… | Header status line, phase-tracker row, and a new change-log entry documenting the correction pass in full.                                                                                                                  |

## Not touched (confirmed via `git status` — nothing outside the above 18 paths is modified)

- No Phase 10, 10A, or 10B file (RPC, service method, migration, or test) was modified.
- No Zone 5 file was touched.
- No UI/hook/component file was touched.
- No Phase 10C migration was edited in place — both corrections are new, forward `CREATE OR REPLACE`/`CREATE POLICY` migrations layered on top of the original 7.
- `apps/web/src/app/actions/warehouse/ambra-location-inventory.ts` was NOT modified — its own legacy raw-write behavior on generic (non-RepairOrder) containers is explicitly preserved and live-proven unaffected by the new RESTRICTIVE policies (T41-T42 in the revised pgTAP suite).
