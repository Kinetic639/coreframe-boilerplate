# Zone 3 Phases 4–6 Corrective Review — Changed File Manifest

Scope: **only** files touched during the 2026-09-11 verify-first corrective
review/fix pass (Findings A–I), diffed against the Phase 4/5/6 implementation
state that existed immediately before this pass began (the state captured in
`docs/mvp/reviews/zone3-phases4-6-review/zone3-phases4-6.diff`). Reconstructed
by applying that baseline diff to `HEAD` in an isolated worktree and diffing
the result against the current working tree — not a hand-picked subset.

12 files total: 2 new, 10 modified. 0 deletions.

| File                                                                                                            | NEW/MODIFIED                              | Finding A–I / shared  | What changed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Tests                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/supabase-target/supabase/migrations/20260911071854_wdd_matcher_sessions_approval_rls_restriction.sql` | **NEW** — **DB migration / RLS change**   | A                     | Replaces `wdd_matcher_sessions`' `wms_update` RLS policy: `USING` now allows either `wdd_matcher.upload` or `.approve` (unchanged reachability); adds a `WITH CHECK` that additionally requires `.approve` specifically whenever the resulting row's `status = 'approved'`.                                                                                                                                                                                                                                                                                                                                                              | The prior policy had no `WITH CHECK` at all and gated purely on `.upload` — an upload-only actor could set `status='approved'` directly via a raw client `.update()`, bypassing `approveSessionAction`'s server-side `.approve` permission check. Confirmed live via `pg_policies` inspection before the fix.                                                                                                                                                                                                                                                                                                        | Verified live via `092_wdd_matcher_approval_rls_test.sql` (see below), run both as inline SQL and as the literal file, as the genuinely-non-bypassing `authenticated` Postgres role. 3/3 assertions passing.                                                                                         |
| `apps/web/supabase/tests/092_wdd_matcher_approval_rls_test.sql`                                                 | **NEW** — **test-only (pgTAP, DB/RLS)**   | A                     | New pgTAP regression test: upload-only actor blocked from directly setting `status='approved'`; the same actor can still perform legitimate non-approval transitions; an approve-only (no `.upload`) actor can still directly approve.                                                                                                                                                                                                                                                                                                                                                                                                   | Permanent live regression coverage for the Finding A RLS fix, run inside `BEGIN…ROLLBACK` against the real dedicated E2E test user so it leaves no residual data.                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 3/3 passing, executed live via Supabase MCP `execute_sql` against `supabase-target` (both as inline SQL and as the file's own literal statement sequence).                                                                                                                                           |
| `apps/web/src/app/actions/tools/wdd-matcher.ts`                                                                 | MODIFIED — **action / permission change** | B, D                  | Imports `WORKSHOP_REPAIR_ORDERS_READ`. `approveAndMaterializeSessionAction`: now loads `context` and derives `organizationId`/`branchId` from it (trusted server context only) for the `materialization_failed` event on the initial-attempt failure path, which previously carried neither field. `retryMaterializationAction`: adds the same `branchId` to its own (pre-existing, `organizationId`-only) `materialization_failed` event emission. `getMaterializationStatusAction`: adds a second permission check (`WORKSHOP_REPAIR_ORDERS_READ`, alongside the existing `wdd_matcher.read` check) plus a doc comment explaining why. | D: audit events for materialization failure must always carry complete, trusted org/branch scope for the audit architecture to be reliable. B: the underlying read is RLS-gated on `workshop.repair_orders.read`, a permission domain independent of `wdd_matcher.read` — without this check, rows hidden by authorization were silently misread as "not yet materialized."                                                                                                                                                                                                                                          | `wdd-matcher-approval-actions.test.ts` (below) — new/updated assertions for both findings.                                                                                                                                                                                                           |
| `apps/web/src/app/actions/tools/__tests__/wdd-matcher-approval-actions.test.ts`                                 | MODIFIED — **test-only**                  | B, D, shared fixtures | `CTX_APPROVE` fixture now includes `workshop.repair_orders.read` (matching a realistic role). New `CTX_MATCHER_READ_ONLY_NO_WORKSHOP_READ` fixture + new test: `getMaterializationStatusAction` rejects a caller with `wdd_matcher.read` but not `workshop.repair_orders.read`, and never calls the underlying service. Added explicit `organizationId`/`branchId` assertions to both existing `materialization_failed` event tests.                                                                                                                                                                                                     | Proves the Finding B and Finding D fixes at the action layer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Part of the 12/12 passing suite (2 new tests).                                                                                                                                                                                                                                                       |
| `apps/web/src/server/services/repair-orders.service.ts`                                                         | MODIFIED — **service change**             | E, G, I               | Adds `REPAIR_ORDER_RPC_SAFE_ERRCODES` allowlist + `normalizeMaterializationRpcError()`; `materializeFromSession`'s error branch now logs the raw RPC error server-side and returns the normalized message instead of the raw one. `listForWorkshop`'s search-value escaping now also double-quotes the ilike value (protecting `,`/`(`/`)`) and escapes `"`/`\`, on top of the existing `%`/`_` escaping. `getMaterializationStatusForSession` now issues a second query against `repair_orders` filtering `deleted_at IS NULL`, excluding soft-deleted RepairOrders from the materialized count/flag.                                   | G: the RPC's raw error text was returned to the client unnormalized, unlike its sibling `approveSession`. E: PostgREST's `.or()` treats unescaped `,`/`(`/`)` as structural syntax, not literal value characters — live-verified against the target Supabase REST endpoint (`HTTP 400 PGRST100` before, `HTTP 200` after, for an identical value). I: matches the `deleted_at IS NULL` convention already used by this service's sibling methods (`listForWorkshop`/`getByIdForWorkshop`); currently unreachable in practice since a downstream RLS policy already hides soft-deleted ROs, kept as defense-in-depth. | `repair-orders.service.test.ts` (below).                                                                                                                                                                                                                                                             |
| `apps/web/src/server/services/__tests__/repair-orders.service.test.ts`                                          | MODIFIED — **test-only**                  | E, G, I               | New/updated tests: 4 errcode-normalization tests for Finding G (3 safe-errcode passthroughs + 1 unrecognized-errcode + 1 codeless-error generic-message case); updated + new search-escaping tests for Finding E (quoted-value expectation, wildcard escaping, comma-injection hardening test); new `makeTableQueryMock` helper + 2 new tests for Finding I (soft-deleted RO excluded; second-query error propagation) plus an updated DISTINCT-count test reflecting the new two-query shape.                                                                                                                                           | Proves the Finding E/G/I service-layer fixes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 25/25 passing (11 new/updated of 25 total in this file).                                                                                                                                                                                                                                             |
| `apps/web/src/hooks/queries/tools/wdd-matcher.ts`                                                               | MODIFIED — **UI/query-hook change**       | F                     | `useApproveAndMaterializeSessionMutation`'s success handler now computes `repairOrderCount` as `createdRepairOrders + reusedRepairOrders` (was `createdRepairOrders` alone), and derives `materialized` from that corrected count being `> 0` (was an always-true `>= 0` check).                                                                                                                                                                                                                                                                                                                                                         | The cache-seed previously undercounted whenever materialization reused an existing RepairOrder instead of creating a new one, inconsistent with `useRetryMaterializationMutation`'s and the component's own already-correct `created + reused` convention.                                                                                                                                                                                                                                                                                                                                                           | Covered by `extraction-review-approval.test.tsx`'s existing mutation-flow assertions (unchanged pass/fail; behavior verified via the corrected numeric expectation implicit in those flows) — no dedicated new hook-level test file exists for this hook; behavior confirmed via the full suite run. |
| `apps/web/src/components/tools/svwms-wdd-matcher/extraction-review-view.tsx`                                    | MODIFIED — **UI change**                  | F                     | Destructures `isError`/`refetch` from `useMaterializationStatusQuery`. New derived `materializationStatusUnavailable` state (approved session, no just-approved result in this tab, status query errored, not loading). Approval-status strip now renders a genuine third visual state (amber, `AlertCircle` icon, "Approved — Status unavailable" copy, manual "Refresh Status" button calling `refetch()`) instead of falling through to the green "Approved" success state when the status query errors.                                                                                                                              | The prior derivation only distinguished "loading" from "materialized: false"; a genuine query error (e.g. the Finding B permission gap, or a transient failure) was indistinguishable from "still loading" once `isLoading` settled false, so it silently rendered as the green success state.                                                                                                                                                                                                                                                                                                                       | Existing `extraction-review-approval.test.tsx` suite re-run and still 7/7 passing (no regression); no new test added specifically for the error-state branch this pass (see Remaining Gaps in `review-context.md`).                                                                                  |
| `apps/web/messages/en.json`                                                                                     | MODIFIED — **i18n, UI-supporting**        | F                     | Adds 3 new keys under `modules.tools.wddMatcher.approval`: `approvedStatusUnavailable`, `statusUnavailableDescription`, `refreshStatus`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Copy for the new Finding F "status unavailable" UI state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | N/A (translation strings; consumed by the component tests indirectly).                                                                                                                                                                                                                               |
| `apps/web/messages/pl.json`                                                                                     | MODIFIED — **i18n, UI-supporting**        | F                     | Polish translations of the same 3 keys.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Same as above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | N/A.                                                                                                                                                                                                                                                                                                 |
| `docs/mvp/zones/03-repair-orders-implementation-plan.md`                                                        | MODIFIED — **tracker/doc-only**           | shared                | Adds a short corrective-review pointer blockquote under each of the Phase 4, 5, and 6 headers, summarizing which findings were fixed/rejected for that phase and linking to the progress tracker for full evidence.                                                                                                                                                                                                                                                                                                                                                                                                                      | Keeps the plan doc's Phase 4/5/6 sections honest about this pass without duplicating the full evidence trail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | N/A (documentation).                                                                                                                                                                                                                                                                                 |
| `docs/mvp/zones/03-repair-orders-progress.md`                                                                   | MODIFIED — **tracker/doc-only**           | shared                | Updates the top-level runtime-status line to mention the corrective-review pass. Adds one evidence bullet under each of Phase 4 (Finding A), Phase 5 (Findings B, D, F, G, I), and Phase 6 (Finding E) with exact before/after/evidence detail. Appends a full new "2026-09-11 (Phase 4/5/6 corrective review)" change-log entry covering all 9 findings, testing, live-authenticated-integration scope, browser-E2E status (unchanged), and documentation/scope-boundary confirmation. Phases 4/5/6 explicitly kept at 🔵 IN PROGRESS, not promoted to ✅ DONE.                                                                         | Live tracker of record must reflect verified results only, per explicit instruction — no phase promoted, no acceptance criteria inflated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | N/A (documentation).                                                                                                                                                                                                                                                                                 |

## Explicitly excluded from this bundle

- `apps/web/next.config.ts`, `apps/web/src/proxy.ts`, `apps/web/src/proxy.test.ts` — unrelated dev-server/Firebase-Studio/proxy work already present in the working tree before this pass; not touched by this correction pass.
- Every file that was part of the original Phase 4–6 implementation but **not** further touched during this correction pass (e.g. `apps/web/src/app/[locale]/dashboard/workshop/page.tsx`, `.../workshop/[id]/page.tsx`, `.../workshop/_components/*`, `apps/web/src/i18n/routing.ts`, `apps/web/src/server/audit/event-registry.ts`, `apps/web/src/server/services/wdd-matcher.service.ts`, `apps/web/src/server/services/__tests__/wdd-matcher.service.test.ts`, `apps/web/src/components/tools/svwms-wdd-matcher/__tests__/extraction-review-approval.test.tsx`) — their content is byte-identical between the pre-correction baseline and the current tree, confirmed by the reconstruction diff producing no hunks for them.
- The original review bundle itself, `docs/mvp/reviews/zone3-phases4-6-review/` (`review-context.md`, `changed-files.md`, `zone3-phases4-6.diff`) — that is the _input_ to this corrective pass (the pre-correction baseline used for reconstruction above), not output of it.
- Container/reservation/allocation/QR/WU/Legacy Stock audit work from earlier sessions — untouched, out of scope for this pass.
