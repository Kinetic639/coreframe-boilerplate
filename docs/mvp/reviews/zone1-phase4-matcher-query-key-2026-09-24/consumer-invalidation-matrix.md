# Zone 1 / Phase 4 — Consumer / Invalidation Matrix

Every real call site touching `wddMatcherKeys.sessions`, found via a full-codebase grep (not assumed from the plan's own "2-3 call sites" estimate — see `implementation-summary.md`).

| Call site                                                                       | Usage type                                                                                     | Old key behavior                                                                                                          | New branch-aware behavior                                                                                                                               | Test coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `wdd-matcher.ts` — `useSessionsQuery`                                           | Query                                                                                          | `queryKey: wddMatcherKeys.sessions()` — one shared bucket for all branches                                                | `queryKey: wddMatcherKeys.sessions(branchId)`, `enabled: !!branchId`                                                                                    | `useSessionsQuery` integration tests (4): distinct cache entries per branch, disabled/no-fetch when null, branchId not forwarded to the request payload, branch switch triggers a new fetch                                                                                                                                                                                                                                                                           |
| `wdd-matcher.ts` — `useCreateAutoSessionMutation`                               | Invalidation                                                                                   | `invalidateQueries({queryKey: wddMatcherKeys.sessions()})`                                                                | `invalidateQueries({queryKey: wddMatcherKeys.sessions(branchId)})`, `branchId` now a required 2nd parameter                                             | Invalidation test: confirms the correct branch key is targeted, confirmed via `vi.spyOn`. **Note: this hook has zero callers anywhere in the codebase (confirmed dead code)** — fixed for consistency and to close the bug class per Section 16, not because any live path exercises it                                                                                                                                                                               |
| `wdd-matcher.ts` — `useRunMatchingMutation`                                     | Invalidation (session list) + invalidation (results, unrelated key, unchanged)                 | `invalidateQueries({queryKey: wddMatcherKeys.sessions()})`                                                                | `invalidateQueries({queryKey: wddMatcherKeys.sessions(branchId)})`, `branchId` now a required 1st parameter                                             | Invalidation test + cross-branch-isolation test (proves a Branch-A run does not invalidate Branch B's cached list)                                                                                                                                                                                                                                                                                                                                                    |
| `wdd-matcher.ts` — `useApproveAndMaterializeSessionMutation`                    | Invalidation (session list) + `setQueryData` (materializationStatus, unrelated key, unchanged) | `invalidateQueries({queryKey: wddMatcherKeys.sessions()})`                                                                | `invalidateQueries({queryKey: wddMatcherKeys.sessions(branchId)})`, `branchId` now a required parameter                                                 | Invalidation test, confirmed via `vi.spyOn`                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `wdd-matcher.ts` — `useRetryMaterializationMutation`                            | N/A — does not touch the session list                                                          | Never invalidated `sessions()`                                                                                            | Unchanged — out of scope, no fix needed                                                                                                                 | N/A (excluded per the task's own "do not invent invalidation for mutations that do not currently affect the list" instruction)                                                                                                                                                                                                                                                                                                                                        |
| `index.tsx` — `useSessionsQuery()` call                                         | Query consumer                                                                                 | `useSessionsQuery()` — no branch awareness                                                                                | `useSessionsQuery(activeBranchId)`, `activeBranchId` read via `useAppStoreV2((s) => s.activeBranchId)` at the component's own top level                 | Covered indirectly via the hook-level tests above; the component itself passes the value through unmodified                                                                                                                                                                                                                                                                                                                                                           |
| `index.tsx` — `runBackgroundPersistence`'s `setQueryData`                       | Cache write                                                                                    | `queryClient.setQueryData(wddMatcherKeys.sessions(), sessionsResult.data)` — writes to the shared, branch-agnostic bucket | `queryClient.setQueryData(wddMatcherKeys.sessions(activeBranchId), sessionsResult.data)` — `activeBranchId` added to the `useCallback` dependency array | Not independently unit-tested (this component's own existing test coverage is limited to `extraction-review-approval.test.tsx`, which does not exercise this code path) — covered by type-check (the call site would not compile without a valid `branchId` argument) and manual reasoning; a UI-level test was judged disproportionate for a same-shape cache-write call already covered by the query/invalidation tests proving the key mechanism itself is correct |
| `index.tsx` — `processFiles`'s `setQueryData`                                   | Cache write (optimistic prepend)                                                               | `queryClient.setQueryData(wddMatcherKeys.sessions(), (current) => [...])` — writes to the shared, branch-agnostic bucket  | `queryClient.setQueryData(wddMatcherKeys.sessions(activeBranchId), (current) => [...])` — `activeBranchId` added to the `useCallback` dependency array  | Same as above                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `extraction-review-view.tsx` — `useRunMatchingMutation()` call                  | Mutation consumer                                                                              | `useRunMatchingMutation()` — no branch awareness                                                                          | `useRunMatchingMutation(activeBranchId)`, `activeBranchId` read via `useAppStoreV2((s) => s.activeBranchId)` at the component's own top level           | Covered indirectly via the hook-level invalidation tests; `extraction-review-approval.test.tsx`'s own existing 14 tests continue to pass unmodified (mocked hooks, real store defaults to `null`, no crash)                                                                                                                                                                                                                                                           |
| `extraction-review-view.tsx` — `useApproveAndMaterializeSessionMutation()` call | Mutation consumer                                                                              | `useApproveAndMaterializeSessionMutation()` — no branch awareness                                                         | `useApproveAndMaterializeSessionMutation(activeBranchId)`                                                                                               | Same as above                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `extraction-review-view.tsx` — direct `listSessionsAction()` call (line ~338)   | Local-state read, NOT a cache read/write                                                       | Calls the server action directly for local `matchedSession`/`view` state; never touches the React Query cache             | **Unchanged — not a consumer of the key factory at all**, confirmed by inspection                                                                       | N/A — out of scope by construction                                                                                                                                                                                                                                                                                                                                                                                                                                    |

## Summary

6 real call sites reconciled across 3 files (`wdd-matcher.ts`, `index.tsx`, `extraction-review-view.tsx`). 1 additional call site (`useRetryMaterializationMutation`) confirmed out of scope (never touched the session list). 1 additional call site (`extraction-review-view.tsx`'s direct `listSessionsAction()`) confirmed not a consumer of the key factory at all.
