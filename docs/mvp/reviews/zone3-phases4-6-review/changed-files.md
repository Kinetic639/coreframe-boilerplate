# Zone 3 — Phases 4–6 — Changed File Manifest

Generated from `git diff --cached` at the time this bundle was prepared. 21 files total: 8 new, 13 modified. Cross-checked against `git status --short` — see `review-context.md` for the verification note.

| File                                                                                                    | Status   | Phase                       | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Tests                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------------------------------------------- | -------- | --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/src/server/services/wdd-matcher.service.ts`                                                   | MODIFIED | Phase 4                     | Rewrote `WddMatcherService.approveSession` to be atomic and race-safe: the `status = 'ready_for_review'` guard is now one of the `.eq()` filters on the UPDATE itself (not a separate SELECT-then-UPDATE), so a concurrent second approval attempt is rejected at the DB level rather than racing. Returns a distinct `SESSION_NOT_READY` error (collapses "not found" and "wrong status" into one message so it never leaks cross-org/branch row existence). Signature changed to accept `branchId: string \| null`.                                                                                                                                                                                                                                                                                             | `apps/web/src/server/services/__tests__/wdd-matcher.service.test.ts` (4 tests)                                                                                                                                                                                                                                       |
| `apps/web/src/server/services/__tests__/wdd-matcher.service.test.ts`                                    | NEW      | Phase 4                     | Unit tests for `approveSession`: ready_for_review→approved succeeds with correct WHERE-clause filters and approved_by/approved_at mapping; other states rejected with `SESSION_NOT_READY`; branch filter omitted when `branchId` is null; DB error normalized. Uses a hand-rolled chainable Supabase mock (`makeChainableUpdateMock`) recording every `.eq()`/`.update()` call for filter-content assertions.                                                                                                                                                                                                                                                                                                                                                                                                     | 4/4 passing                                                                                                                                                                                                                                                                                                          |
| `apps/web/src/server/services/repair-orders.service.ts`                                                 | MODIFIED | Phase 5 / Phase 6           | Added four exports: `MaterializationStatus`, `RepairOrderListRow` types; `getMaterializationStatusForSession` (Phase 5 read model — counts distinct `repair_order_id`s linked via `workshop_source_documents`/`repair_order_source_document_links` for a session, so "approved but zero linked repair_orders" can be read back without re-running materialization); `listForWorkshop` (Phase 6 — org/branch-scoped list with a single OR-ILIKE search across `zl_number`/`vin`/`order_number`, joins `crm_contacts` for `advisorDisplayName`); `getByIdForWorkshop` (Phase 6 — single-row detail fetch, org/branch-scoped, returns `null` rather than an error for a not-found/wrong-branch id).                                                                                                                  | `apps/web/src/server/services/__tests__/repair-orders.service.test.ts` (14 new tests: 5 for `listForWorkshop`, 2 for `getMaterializationStatusForSession`, 2 for `getByIdForWorkshop`, plus mapping/error-path cases)                                                                                                |
| `apps/web/src/server/services/__tests__/repair-orders.service.test.ts`                                  | MODIFIED | Phase 5 / Phase 6           | Added the 14 tests above and a new `makeChainableQueryMock` helper (supports both `.maybeSingle()` and direct-`await` termination shapes) alongside the pre-existing Phase 3 `materializeFromSession` tests (unchanged).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 14/14 new tests passing; 5 pre-existing Phase 3 tests unaffected                                                                                                                                                                                                                                                     |
| `apps/web/src/app/actions/tools/wdd-matcher.ts`                                                         | MODIFIED | Phase 4 / Phase 5           | Added: `approveSessionAction` (Phase 4 — permission-gated on `wdd_matcher.approve`, zod-validated via the pre-existing `approveSessionSchema`, emits `workshop.matcher_session.approved`); `ApproveAndMaterializeResult` type + `approveAndMaterializeSessionAction` (Phase 5 — calls `approveSessionAction` then, only on its success, `RepairOrdersService.materializeFromSession` as a genuinely separate call; on materialization failure returns `success:true` with `materializationError` set, never a thrown/failed action, and emits `workshop.repair_orders.materialization_failed`); `retryMaterializationAction` (Phase 5 — re-invokes the same idempotent Phase 3 RPC, gated on `wdd_matcher.approve`); `getMaterializationStatusAction` (Phase 5 — read-model action, gated on `wdd_matcher.read`). | `apps/web/src/app/actions/tools/__tests__/wdd-matcher-approval-actions.test.ts` (12 tests)                                                                                                                                                                                                                           |
| `apps/web/src/app/actions/tools/__tests__/wdd-matcher-approval-actions.test.ts`                         | NEW      | Phase 4 / Phase 5           | Action-layer tests for all four new actions: approval success + event emission; unauthorized/unauthenticated/malformed-input rejection; `SESSION_NOT_READY` propagation; approve+materialize full success; materialization failure surfaced as `materializationError` (not an action failure) with the failure event emitted; approval failure short-circuits before materialization is attempted; retry calls the same service method and is permission-gated; retry failure emits the failure event with `attempt: "retry"`; status read model returns correctly. Mocks `createClient`, `loadDashboardContextV2`, `WddMatcherService`, `RepairOrdersService`, `eventService` per the repo's existing `tools-actions.test.ts` convention.                                                                        | 12/12 passing                                                                                                                                                                                                                                                                                                        |
| `apps/web/src/server/audit/event-registry.ts`                                                           | MODIFIED | Phase 4 / Phase 5           | Registered two new platform events under the existing "Workshop module — Zone 3 RepairOrders" section: `workshop.matcher_session.approved` (metadata: `previousStatus`, `newStatus`, `branchId`) and `workshop.repair_orders.materialization_failed` (metadata: `errorClass`, `errorMessage` [marked `sensitiveFields`], `attempt: "initial" \| "retry"`). Follows the exact shape of the pre-existing `workshop.repair_orders.materialized` entry (unchanged).                                                                                                                                                                                                                                                                                                                                                   | Covered indirectly via the action tests above, which assert `eventService.emit` is called with these `actionKey`s and metadata shapes.                                                                                                                                                                               |
| `apps/web/src/components/tools/svwms-wdd-matcher/extraction-review-view.tsx`                            | MODIFIED | Phase 4 / Phase 5           | Added the Approve button (rendered only when `matchedSession.status === 'ready_for_review'` AND the caller's permission snapshot allows `wdd_matcher.approve` — client-side UX only, the real gate is server-side in the action) and a new "approval status strip" block (rendered when `status === 'approved'`): shows a materialized/repair-order-count success state with a "Go to Workshop" link, or a "Materialization failed" state with a working Retry button, derived from either the just-completed mutation's own result or the `getMaterializationStatusForSession` read model on reload. New imports: `usePermissions`, `PERMISSION_WDD_MATCHER_APPROVE`, `Link` (from `@/i18n/navigation`), three new hooks, `AlertTriangle`/`RotateCcw` icons.                                                     | `apps/web/src/components/tools/svwms-wdd-matcher/__tests__/extraction-review-approval.test.tsx` (7 tests)                                                                                                                                                                                                            |
| `apps/web/src/components/tools/svwms-wdd-matcher/__tests__/extraction-review-approval.test.tsx`         | NEW      | Phase 4 / Phase 5           | Component tests, narrowly scoped to the header/approval-status-strip (mocks the hook module, `usePermissions`, `next/dynamic`, and `next-intl` wholesale — matches the existing `tool-detail-client.test.tsx` mocking convention; wraps renders in a real `QueryClientProvider` since the component calls the real `useQueryClient()`). Covers: button shown only when ready_for_review + permitted; button hidden without permission; no control at all for other statuses (e.g. `processing`); mutation called on click + loading state; "Approved" success state with no duplicate approve control; "Materialization failed" state with a working Retry button; the just-approved failure state rendered immediately without waiting on the status query.                                                      | 7/7 passing                                                                                                                                                                                                                                                                                                          |
| `apps/web/src/hooks/queries/tools/wdd-matcher.ts`                                                       | MODIFIED | Phase 4 / Phase 5           | Added `wddMatcherKeys.materializationStatus(sessionId)` query key; `useMaterializationStatusQuery` (enabled only for approved sessions); `useApproveAndMaterializeSessionMutation` (invalidates the sessions list, seeds the materialization-status cache from the mutation result, shows a success/failure toast depending on `materializationError`); `useRetryMaterializationMutation` (updates the materialization-status cache on success, shows a success toast).                                                                                                                                                                                                                                                                                                                                           | Exercised indirectly through the component tests above (the hooks module is mocked there); no separate hook-level unit test file added.                                                                                                                                                                              |
| `apps/web/src/app/[locale]/dashboard/workshop/page.tsx`                                                 | MODIFIED | Phase 6                     | Replaced the entire "coming soon" feature-card placeholder with a real, server-rendered RepairOrder list: permission gate corrected from the placeholder's generic `WORKSHOP_READ` to `WORKSHOP_REPAIR_ORDERS_READ` (matching the live RLS policy on `repair_orders`); reads `?q=` from `searchParams`; calls `RepairOrdersService.listForWorkshop` directly (server component, no action needed); renders a desktop `<Table>` (`hidden md:block`) and a mobile card list (`md:hidden`); each row links to the new `/dashboard/workshop/[id]` detail route; renders `EmptyState` (async component, distinguishes "no orders yet" vs. "no search results") or a load-error message.                                                                                                                                | Covered via `RepairOrdersSearch`/`RepairOrderStatusBadge` component tests (search box and status rendering) and the `listForWorkshop` service tests (query/scoping logic); the page's own inline list/table/empty-state JSX is not independently RTL-tested — see `review-context.md`'s "Known unresolved findings". |
| `apps/web/src/app/[locale]/dashboard/workshop/[id]/page.tsx`                                            | NEW      | Phase 6                     | Minimal RepairOrder detail shell ("option A" — identifier/context only: zl_number, order_number, VIN, status, identity_status, advisor, created/updated timestamps). Deliberately not Phase 7's full header/lines/lifecycle view. Same permission gate as the list page; calls `RepairOrdersService.getByIdForWorkshop`; `notFound()` on a missing/wrong-branch id.                                                                                                                                                                                                                                                                                                                                                                                                                                               | Covered indirectly via `getByIdForWorkshop`'s service tests; no dedicated RTL test for this page.                                                                                                                                                                                                                    |
| `apps/web/src/app/[locale]/dashboard/workshop/_components/repair-orders-search.tsx`                     | NEW      | Phase 6                     | Client component: debounced (350ms) search input driving the `?q=` URL param via `router.replace`; clears the param entirely (not to an empty string) when the input is emptied.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `apps/web/src/app/[locale]/dashboard/workshop/_components/__tests__/repair-orders-search.test.tsx` (3 tests)                                                                                                                                                                                                         |
| `apps/web/src/app/[locale]/dashboard/workshop/_components/__tests__/repair-orders-search.test.tsx`      | NEW      | Phase 6                     | Tests: initial query pre-filled; debounced navigation to `?q=<value>` after typing (fake timers); clearing removes the `q` param entirely rather than setting it to `""`. Mocks `next/navigation`'s `useRouter`/`usePathname`/`useSearchParams` and `next-intl`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 3/3 passing                                                                                                                                                                                                                                                                                                          |
| `apps/web/src/app/[locale]/dashboard/workshop/_components/repair-order-status-badge.tsx`                | NEW      | Phase 6                     | Small presentational badge mapping `RepairOrderStatus` (`open`/`closed`/`archived`) to a shadcn `Badge` variant + translated label, with a graceful fallback for an unrecognized status value.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `apps/web/src/app/[locale]/dashboard/workshop/_components/__tests__/repair-order-status-badge.test.tsx` (4 tests)                                                                                                                                                                                                    |
| `apps/web/src/app/[locale]/dashboard/workshop/_components/__tests__/repair-order-status-badge.test.tsx` | NEW      | Phase 6                     | Tests: a label renders for each of the three known statuses; an unknown status value renders its own raw value rather than crashing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 4/4 passing                                                                                                                                                                                                                                                                                                          |
| `apps/web/src/i18n/routing.ts`                                                                          | MODIFIED | Phase 6                     | Registered the new dynamic route in next-intl's `pathnames` map: `"/dashboard/workshop/[id]"` → `en: "/dashboard/workshop/[id]"`, `pl: "/dashboard/warsztat/[id]"` (the `[id]` segment itself stays a literal placeholder in both locales, matching the existing convention used for `/dashboard/warehouse/audits/[id]/...`). Required for the new detail route's typed `<Link>` usage to type-check and resolve correctly under the existing localized-path scheme.                                                                                                                                                                                                                                                                                                                                              | No dedicated test; exercised implicitly by `pnpm type-check` passing (the typed-routes literal union includes the new pathname) and by the detail page existing/being reachable.                                                                                                                                     |
| `apps/web/messages/en.json`                                                                             | MODIFIED | Phase 4 / Phase 5 / Phase 6 | Added `modules.tools.wddMatcher.approval.*` (12 keys: approveButton, approving, approveSuccessToast, materializationFailedToast, retrySuccessToast, approvedAndMaterialized, approvedMaterializationFailed, checkingStatus, repairOrdersCount [ICU plural], retryMaterialization, retrying, goToWorkshop) and `modules.workshop.repairOrders.*` (title, subtitle, openRepairOrder, search.{placeholder,clear}, columns._, status._, identityStatus._, emptyState._, errors.loadFailed, detail.\*).                                                                                                                                                                                                                                                                                                                | JSON validity checked via `node -e "JSON.parse(...)"`; string usage exercised indirectly through the component tests that mock `useTranslations`.                                                                                                                                                                    |
| `apps/web/messages/pl.json`                                                                             | MODIFIED | Phase 4 / Phase 5 / Phase 6 | Polish translations for the same two key groups as `en.json` above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Same as `en.json`.                                                                                                                                                                                                                                                                                                   |
| `docs/mvp/zones/03-repair-orders-implementation-plan.md`                                                | MODIFIED | Shared (tracker)            | Phase 4/5/6 headers annotated `🔵 IN PROGRESS`; every implementation-task checkbox honestly checked or left unchecked against what was actually built/tested this session, each with an inline evidence note (including explicit notes on the two tasks marked N/A by design — server action and query hook, both superseded by the work order's own sanctioned URL-search-param + direct-service-call alternative); each phase's "Testing requirements" and "Acceptance criteria" sections rewritten to state precisely what is and is not covered, with the outstanding genuine-browser-E2E gap stated plainly in all three phases (not silently implied as covered).                                                                                                                                           | N/A (documentation)                                                                                                                                                                                                                                                                                                  |
| `docs/mvp/zones/03-repair-orders-progress.md`                                                           | MODIFIED | Shared (tracker)            | Header/runtime-status updated; overall task counts recomputed (62/164, +18: Phase 4 +8, Phase 5 +6, Phase 6 +4); phase-tracker table rows for 4/5/6 updated to `🔵 IN PROGRESS` with evidence summaries; new detailed per-task breakdown sections for Phases 4/5/6 (mirroring the existing Phase 3 section's style) added after the Phase 3 section; DEMO READY gate items annotated inline with "live-verified" vs. "not verified" status without being checked off; a new dated change-log entry describing the full session (Phase 4/5/6 build, live-integration-verification methodology, the extensive Playwright/browser troubleshooting and its outcome).                                                                                                                                                  | N/A (documentation)                                                                                                                                                                                                                                                                                                  |

## Totals

- **New files:** 8 (`wdd-matcher.service.test.ts`, `wdd-matcher-approval-actions.test.ts`, `extraction-review-approval.test.tsx`, `workshop/[id]/page.tsx`, `repair-orders-search.tsx` + its test, `repair-order-status-badge.tsx` + its test)
- **Modified files:** 13
- **Total:** 21
