# Dashboard Integration Notes

## What main actually has now

`main` (via `da512d18`/`158fa79a`/`d3e80c4e`, "feat(dashboard): refine operational home dashboard") has rebuilt `/dashboard/start` from the 15-line static placeholder into a real, server-rendered operational dashboard: active org/branch context, a real ticket-attention queue (`HelpdeskTicketsService.listForDataView`), a real personal-activity feed, a Planning widget (today's calendar, branch-scoped open/in-progress tasks, first visible Kanban board), scoped `Suspense` boundaries, and a single new client boundary (`HomeScopeBoundary`) for branch-context sync + manual refresh. Verification recorded on main: 38 Vitest tests, scoped ESLint, full `tsc --noEmit`, 6 Playwright tests against live data, all 15×2 (light/dark) skin combinations, and 9 responsive breakpoints. Main's own doc update rates the zone **🟡 PARTIAL — IMPLEMENTED AND TESTED; GLOBAL BUILD GATE OPEN**, blocked only by one explicitly-unrelated, explicitly-pre-existing full-`next build` failure (`apps/web/src/app/auth/auth-code-error/page.tsx` missing a root layout) that this same main-side work explicitly disclaims responsibility for and does not fix.

## Do NOT overwrite this with the working branch's placeholder

Zone 1's own branch never touched `/dashboard/start/page.tsx` or any of its `_components`/`_lib` files at all (confirmed via `git diff --name-only` — zero hits under `dashboard/start/` on the working-branch-only file list). **There is no runtime conflict here and no risk of accidentally overwriting main's rebuilt dashboard** — the only touchpoint is the documentation file `docs/mvp/zones/11-home-operational-dashboard.md`, which Zone 1's own branch also edited (to add a "CURRENT STATUS — DEMO SUFFICIENT" banner, based on the OLD placeholder state).

## Adjacent branch-switch/context files Zone 1 touched — do they need combining with the dashboard work?

Checked explicitly, per the task's own instruction. Zone 1 Phase 1 touched `SidebarBranchSwitcher.tsx` (the sidebar's own branch-switch handler, `router.replace("/dashboard/start")` + `router.refresh()`) — this is **upstream** of the dashboard content itself: it navigates TO `/dashboard/start`, it does not render anything on that page. Main's own dashboard rebuild's new `HomeScopeBoundary` client component handles branch-context sync WITHIN the page once landed there. These two mechanisms are complementary, not overlapping: Phase 1's fix ensures the user actually lands on a fresh `/dashboard/start` after a switch (via `router.refresh()`); main's `HomeScopeBoundary` ensures that once there, the page's own widgets re-scope to the new branch. No file-level conflict exists between them (confirmed: `sidebar-branch-switcher.tsx` does not appear in the 14-file overlap list at all), and no combination work is needed — they are two different layers of the same overall "branch switch works correctly" property, each already verified independently (Phase 1's own regression tests; main's own Playwright branch-switch-and-restore test, per its readiness-progression checklist: "zmiana i przywrócenie oddziału bez fatalnych błędów konsoli").

## Final intended dashboard state (explicit, per the task's own requirement)

After integration, `/dashboard/start` should be **exactly main's own rebuilt operational dashboard**, entirely unmodified by anything from the Zone 1 branch — Zone 1 contributes nothing to this route's own runtime code and should take none of it away. The only dashboard-adjacent Zone 1 contribution that survives is `SidebarBranchSwitcher.tsx`'s own `router.replace("/dashboard/start")` + `router.refresh()` fix (a different file, no conflict), which continues to correctly deliver the user to this now-much-better page after a branch switch.

## The one required documentation reconciliation

`docs/mvp/zones/11-home-operational-dashboard.md` needs a genuine combine (see `three-way-resolution-matrix.md` for the file-level detail), not a pick-one-side resolution:

1. **Take main's rebuild record wholesale** (status, decisions, architecture, readiness checklist, known build-gate gap) — it is accurate and extensively verified; Zone 1's competing claim that the placeholder is unchanged is simply wrong post-integration.
2. **Preserve, but re-date/re-contextualize, Zone 1's own "demo-sufficient" decision record** — the underlying historical fact (a product owner accepted the OLD placeholder as pitch-sufficient on 2026-09-24) remains true and worth keeping as a dated historical note, but the "CURRENT STATUS" framing needs to change from "still true today" to "superseded by an even better actual dashboard, landed independently on `main` — this only strengthens pitch readiness, it does not need to be re-decided as a rejection."
3. **Flag, do not silently resolve, one product question this integration surfaces**: the original "presenter must not linger on the dashboard" guidance (`presentation-ready-gate.md`'s own "Explicitly NOT required" section, re: the notification bell) was calibrated against the OLD placeholder. With a real, tested dashboard now available for free via this integration, the product owner may want to reconsider showcasing it rather than avoiding it — this is a genuine open question this audit surfaces, not a decision this audit makes.
4. **Companion cleanup (non-conflicting, same-topic):** `docs/mvp/reviews/dashboard-container-product-clarification-2026-09-24/dashboard-current-status.md` (a Zone-1-only file, no git conflict, since main never touched it) makes the same now-stale "15-line placeholder" claim. It won't show up as a merge conflict because only one side ever touched it, but it should be revisited in the same documentation pass so the repo doesn't end up with two internally-contradictory "current status" claims about the same screen.
