# Zone 1 Behavior Preservation Verdict

Per the task's own explicit list (Section 14): verified each area's files are untouched by the merge (no conflict resolution needed) and re-confirmed their own signature markers are present and their own tests pass post-integration.

| Area                          | Files                                                | Conflict resolution needed?                                                    | Post-merge verification                                                                                                                                                                                                |
| ----------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SidebarBranchSwitcher         | `sidebar-branch-switcher.tsx`                        | No — untouched by main                                                         | `router.replace`/`router.refresh` present (2 refs, grep-confirmed); `sidebar-branch-switcher.test.tsx` passes (5/5)                                                                                                    |
| `changeBranch`                | `changeBranch.ts`                                    | No — untouched by main                                                         | `isBranchAccessible` present (2 refs); `changeBranch.test.ts` passes (all)                                                                                                                                             |
| `isBranchAccessible`          | `branch-access.ts`                                   | No — untouched by main                                                         | Definition present (1 ref, grep-confirmed)                                                                                                                                                                             |
| PermissionsSync               | `permissions-sync.tsx`                               | No — untouched by main                                                         | `permissions-sync.test.tsx` passes (all)                                                                                                                                                                               |
| Branch permissions query      | `use-branch-permissions-query.ts`                    | No — untouched by main                                                         | `use-branch-permissions-query.test.tsx` passes (all)                                                                                                                                                                   |
| Matcher branch-aware sessions | `wdd-matcher.ts` + tests                             | No — untouched by main                                                         | `wddMatcherKeys` present (19 refs, grep-confirmed); `wdd-matcher.test.ts`, `extraction-review-approval.test.tsx`, `movement-import-boundary.test.ts` all pass                                                          |
| QR resolver                   | `public-token-resolver.ts`, `target-registry.ts`     | No — untouched by main                                                         | "Correction A"/"Correction B" comments present (3 refs, grep-confirmed); `public-token-resolver.test.ts` passes (13/13)                                                                                                |
| Cross-branch Locations dialog | `ambra-locations-client.tsx`                         | Auto-merged cleanly, verified no incompatibility (see `resolved-conflicts.md`) | `ambra-locations-client.cross-branch.test.tsx` passes (7/7, after the unrelated `organizationId` fixture fix) — confirm/cancel/`changeBranch`/exact-target-preservation/failed-switch/no-silent-switch all re-verified |
| Sign-in QR return flow        | `sign-in-form.tsx`, sign-in `page.tsx`, `actions.ts` | No — untouched by main                                                         | `sign-in-form.test.tsx`, sign-in `page.test.tsx` pass                                                                                                                                                                  |

## Result: no unexpected change to any of these areas

Every file in this list was either (a) completely absent from the 14-file overlap list — meaning the merge could not have touched it at all — or (b) `ambra-locations-client.tsx`, which auto-merged cleanly and was explicitly verified (not assumed) to still contain every one of Phase 6's behaviors: pending cross-branch state, confirm/cancel, the `changeBranch` call, exact-target preservation via `router.replace`'s explicit `query.selected`, failed-switch handling (dialog stays open, no false branch state), the Radix `event.preventDefault()` fix, and no-silent-switch-on-mount. All re-proven by its own full test suite passing unchanged.

**None of these areas required a conflict resolution decision** — they simply were not part of the conflict surface, exactly as the original audit predicted in `zone1-protection-notes.md`.
