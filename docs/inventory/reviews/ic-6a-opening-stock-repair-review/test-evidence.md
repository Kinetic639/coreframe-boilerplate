# IC-6A Test Evidence

## New Vitest scenarios (inventory-products.service.test.ts)

New `describe("InventoryProductsService.createEnhancedProduct —
opening stock (IC-6A)")` block, 5 tests, all passing:

- **A**: `opening_quantity = 0` → `inventory_create_and_finalize` never
  called.
- **B**: `opening_quantity = 5` → `inventory_create_and_finalize`
  called once with `p_organization_id`/`p_branch_id`/`p_movement_type_
code: '401'`/`p_actor_user_id`/`p_external_reference: productId`/
  `p_lines: [{variant_id, destination_location_id, unit_id,
quantity: 5}]` — and explicit assertions that the OLD broken RPC
  names (`inventory_create_draft_movement`, `inventory_post_movement`)
  are never called.
- **C**: a canonical RPC error (`{data: null, error: {message:
"Not authorized..."}}`) propagates as `result.success === false`
  with the real error message, AND triggers the existing compensating
  cleanup (`inventory_variants`/`inventory_products` archived-status
  updates observed in the mock's own operation log).
- **D**: two variants, opening quantities 5 and 12 — one RPC call, two
  independent `p_lines` entries with exact per-variant quantities.
- **E**: a mixed zero/nonzero-quantity variant list (first variant
  quantity 0, second quantity 7) — proves the SECOND variant's own id
  is correctly used for its own line (not shifted/mismatched by the
  first variant's own filtering-out).

Focused run: `npx vitest run src/server/services/__tests__/
inventory-products.service.test.ts` — **15/15 passed** (10 pre-
existing + 5 new), 0 failures.

## New pgTAP file: `109_ic6a_opening_stock_repair_test.sql`

`plan(16)`, live result: **16/16, 0 failures.**

Proves the exact call shape `createOpeningStockMovement` now sends,
live, against the real canonical engine (not a mock):

- **A1**: no balance row exists before the movement (`on_hand`
  implicitly 0).
- **B1/B2**: `inventory_create_and_finalize` returns `status = 'posted'`;
  `on_hand_quantity` becomes exactly 5.
- **C1–C4**: exactly one new movement header; `movement_type_code =
'401'`; `external_reference` carries the product id; a real
  `document_number` was generated by the canonical numbering path.
- **D1–D3**: exactly one new ledger entry; `direction = 'increase'`;
  `quantity = 5`.
- **E1–E2**: `posted_by` is the real actor; exactly one `'posted'`
  audit-log row attributed to the real actor.
- **F1–F4**: retrying the IDENTICAL request (same idempotency key) a
  second time returns `status = 'posted'` again (not an error);
  `on_hand_quantity` remains exactly 5 (not 10); the movement-header
  count and ledger-entry count both remain unchanged from after the
  first call — proving retry-safety live, not merely by code-reading.

One fixture bug self-caught and fixed during authoring: temp tables
created before `SET LOCAL ROLE authenticated` (`fake_product`,
`movement_count_before`, `ledger_count_before`) need an explicit
`GRANT SELECT ... TO authenticated`, same as every other fixture table
in this project's established pgTAP convention — omitted on the first
draft, added after the live run surfaced `42501: permission denied`.

## Full regression re-run: 097–108

Delegated to a background agent, executed live in numeric order, each
file its own `BEGIN...ROLLBACK` transaction, verbatim (no SQL modified
before execution). Confirms the IC-6A TypeScript-only change (no
migration) caused zero SQL-layer regression:

| File | Assertions | not_ok | Status |
| ---- | ---------- | ------ | ------ |
| 097  | 29         | 0      | PASS   |
| 098  | 17         | 0      | PASS   |
| 099  | 20         | 0      | PASS   |
| 100  | 44         | 0      | PASS   |
| 101  | 17         | 0      | PASS   |
| 102  | 11         | 0      | PASS   |
| 103  | 35         | 0      | PASS   |
| 104  | 44         | 0      | PASS   |
| 105  | 29         | 0      | PASS   |
| 106  | 87         | 0      | PASS   |
| 107  | 46         | 0      | PASS   |
| 108  | 23         | 0      | PASS   |

**Total: 402/402, 0 failures, 12/12 files PASS.**

`105_ic7a_movement_engine_security_boundary_test.sql` (29/29) is the
directly relevant security-boundary proof for this pass's own new RPC
usage — it proves anon has zero EXECUTE on `inventory_create_and_
finalize` and friends, replays the original P0 exploit as anon
(rejected), and proves actor-impersonation/NULL-actor/no-permission
rejection, all against the SAME hardened entry point the IC-6A fix now
calls. `102_ic1_reserved_only_hard_invariant_test.sql` (11/11) directly
exercises `inventory_create_and_finalize` for movement types 401/402,
confirming the hard stock invariant remains active for this exact call
path.

**Combined IC-6A-relevant pgTAP total (402 regression + 16 new):
418/418, 0 failures.**

## Full Vitest re-run

**4456 passed / 32 failed / 8 skipped / 9 todo (4505 total).** The 32
failures are identical in count and (spot-checked via `git stash`
during IC-6, unchanged surface this pass) nature to the pre-existing,
unrelated baseline established during IC-6 — auth/sign-up,
invitations, org roles/members/positions UI, mobile menu/header,
sidebar registries, QR label/ZPL rendering, admin entitlements, org
RLS branch-creation. None touch `inventory-products.service.ts` or its
test file. The +5 over IC-6's own 4500-test baseline are exactly this
pass's own 5 new opening-stock tests, all passing.

## Static checks

- `pnpm type-check`: 0 errors.
- `pnpm lint`: 0 errors, 319 pre-existing warnings (unchanged
  baseline).
- `pnpm build`: succeeded cleanly — required and run, since this pass
  changes real application/service TypeScript.
- `git diff --check`: clean.

## Zero residual test data

All pgTAP fixtures ran inside `BEGIN...ROLLBACK`. `109_...`'s own
branch number (700) is documented here for future test-authoring
collision avoidance, matching this project's established convention.

## Concurrency

No dedicated concurrency test was added or is required — IC-6A is a
pure caller-migration (moving one TypeScript call site onto an
existing, unchanged canonical RPC), introducing no new locking or
concurrency behavior. The canonical RPC's own existing concurrency
properties (proven in earlier IC phases) are unaffected and were not
re-tested here.
