# IC-4 — Changed Files

**Branch**: `zone3-zone5-integration-audit`.
**Baseline SHA**: `97f0e9d923cefbf9043eaefcab25d4c1cf9315b1` (commit `ic7a`)
— confirmed clean working tree at this exact commit before any IC-4 work
began.

**Updated by the IC-4 correction pass (2026-09-16)**: added migration
13, extended `106_...`'s own pgTAP coverage, fixed the audit-event
`partial` flag, added a new focused Vitest file. See the bottom of this
file for the correction pass's own file list.

## New files (13)

| File                                                                                                   | Purpose                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/supabase-target/supabase/migrations/20260916065129_ic4_branch_transfer_status_lifecycle.sql` | New status CHECK (`prepared`/`in_transit`/`accepted`/`partially_accepted`/`declined`/`cancelled`); default status now `prepared`; new `cancelled_by`/`cancelled_at`/`cancel_reason` columns.                                                                                                                                                                                                                          |
| `.../20260916065136_ic4_branch_transfer_lines_sent_accepted_quantity.sql`                              | New `sent_quantity`/`accepted_quantity` columns on transfer lines, with the `0 <= accepted <= sent` CHECK.                                                                                                                                                                                                                                                                                                            |
| `.../20260916065147_ic4_branch_transfer_discrepancies_table.sql`                                       | New `inventory_branch_transfer_discrepancies` table, RLS (scoped SELECT, RPC-only write).                                                                                                                                                                                                                                                                                                                             |
| `.../20260916065209_ic4_fix_redundant_id_org_unique_constraint.sql`                                    | Self-caught corrective: drops a UNIQUE constraint duplicating a pre-existing index.                                                                                                                                                                                                                                                                                                                                   |
| `.../20260916065249_ic4_seed_movement_type_312_transfer_in.sql`                                        | New movement type `312` (Inter-Branch Transfer In); `311` flipped to `allows_manual_entry=false`; backfilled for all 4 existing orgs.                                                                                                                                                                                                                                                                                 |
| `.../20260916065315_ic4_rebuild_inventory_create_branch_transfer.sql`                                  | Status now `prepared` (not `in_transit`); actor-identity check added; converted to `SECURITY DEFINER`.                                                                                                                                                                                                                                                                                                                |
| `.../20260916065326_ic4_branch_transfer_tables_restrictive_rls.sql`                                    | Closes the raw-write RLS gap on `inventory_branch_transfers`/`_lines` (RESTRICTIVE deny INSERT/UPDATE/DELETE for `authenticated`).                                                                                                                                                                                                                                                                                    |
| `.../20260916065347_ic4_inventory_send_branch_transfer.sql`                                            | New RPC: posts the real `311` physical movement, consumes the reservation exactly once, atomically.                                                                                                                                                                                                                                                                                                                   |
| `.../20260916065426_ic4_rebuild_inventory_accept_branch_transfer.sql`                                  | Full rebuild: routes through the canonical engine (`312`), supports full/partial accept, persists discrepancies, idempotent retry. **Superseded by migration 13 (correction pass) below.**                                                                                                                                                                                                                            |
| `.../20260916065435_ic4_drop_stale_accept_branch_transfer_overload.sql`                                | Self-caught corrective: drops the stale, still-`anon`-exploitable 3-arg overload left behind by the arity change above.                                                                                                                                                                                                                                                                                               |
| `.../20260916065452_ic4_rebuild_inventory_decline_branch_transfer.sql`                                 | Full rebuild: pre-shipment only, no automatic return movement.                                                                                                                                                                                                                                                                                                                                                        |
| `.../20260916065504_ic4_inventory_cancel_branch_transfer.sql`                                          | New RPC: source-side pre-shipment cancel, distinct from decline.                                                                                                                                                                                                                                                                                                                                                      |
| `apps/web/supabase/tests/106_ic4_branch_transfer_test.sql`                                             | pgTAP file, now 87/87 (see correction pass below) — full lifecycle, partial accept + discrepancy, pre-send decline/cancel, post-send rejection, NULL-actor, impersonation, no-permission, cross-org, same-branch, anon negatives, raw-write denial, idempotent retry, IC-1 commitment protection, same-SKU independence, reversal compatibility, 100%%-missing (no orphan draft), strict explicit-payload validation. |

## IC-4 correction pass (2026-09-16) — new/modified files

| File                                                                                                              | Change                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `apps/web/supabase-target/supabase/migrations/20260916170438_ic4c_fix_orphan_draft_and_strict_partial_accept.sql` | **New.** `CREATE OR REPLACE` on `inventory_accept_branch_transfer` (same 4-arg signature): fixes the orphan-312-draft BLOCKER (header only created if `total_accepted > 0`) and makes the explicit `p_line_acceptances` payload exact/fail-closed (validated in full before any mutation).                                                                         |
| `apps/web/supabase/tests/106_ic4_branch_transfer_test.sql`                                                        | **Extended**, not replaced: `plan(60)` → `plan(87)`. New Scenario Q (16 assertions — 100%%-missing receipt, no orphan header, discrepancy `destination_movement_id` NULL, idempotent retry) and Scenario R (11 assertions — missing/unknown/duplicate/foreign-transfer line-id rejection, zero-mutation proof, exact-complete full and partial explicit payloads). |
| `apps/web/src/app/actions/warehouse/inventory/index.ts`                                                           | `acceptInventoryBranchTransferAction`'s own audit-event `partial` flag now derived from the RPC's own result `status` (`=== 'partially_accepted'`), not from `Boolean(parsed.data.line_acceptances?.length)`.                                                                                                                                                      |
| `apps/web/src/app/actions/warehouse/inventory/__tests__/branch-transfer-accept-partial-event.test.ts`             | **New.** 4 focused Vitest cases proving the `partial` flag tracks the RPC's own result status in all four input/outcome combinations.                                                                                                                                                                                                                              |

## Modified files (7)

| File                                                           | Change                                                                                                                                                                                                                                                                               |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `apps/web/src/server/services/inventory-enterprise.service.ts` | New `sendBranchTransfer`/`cancelBranchTransfer` methods; `acceptBranchTransfer` extended with an optional `lineAcceptances` parameter (partial-accept support). `createBranchTransfer`/`listBranchTransfers` unchanged.                                                              |
| `apps/web/src/app/actions/warehouse/inventory/schemas.ts`      | New `sendBranchTransferSchema`/`cancelBranchTransferSchema`; `acceptBranchTransferSchema` extended with optional `line_acceptances`.                                                                                                                                                 |
| `apps/web/src/app/actions/warehouse/inventory/index.ts`        | New `sendInventoryBranchTransferAction`/`cancelInventoryBranchTransferAction`; `acceptInventoryBranchTransferAction` passes `line_acceptances` through. `createInventoryBranchTransferAction`/`declineInventoryBranchTransferAction`/`listInventoryBranchTransfersAction` unchanged. |
| `apps/web/src/server/audit/event-registry.ts`                  | New `warehouse.inventory.branch_transfer.sent`/`.cancelled` event definitions; `.accepted`'s own metadata schema gained an optional `partial` flag.                                                                                                                                  |
| `docs/inventory/inventory-core-architecture.md`                | §9's own `inventory_branch_transfers`/`_lines` RLS row updated to CLOSED; new §9B ("IC-4 — Branch Transfer / MMJ Rebuild") added; two inline pointer notes added to the pre-existing pre-IC-1 audit narrative (not rewritten, only annotated).                                       |
| `docs/inventory/inventory-core-implementation-plan.md`         | IC-4 section marked DONE; the pre-existing "in transit means reserved" planning text (written before the product decision existed) preserved for historical record but explicitly marked superseded, with a pointer to the final as-implemented design.                              |
| `docs/inventory/inventory-core-progress.md`                    | Runtime status → IC-4 DONE; phase-tracker row updated; full IC-4 change-log entry appended; overall-execution summary updated.                                                                                                                                                       |

## No changes to

- `inventory_reverse_movement`, `inventory_finalize_posting`/`_internal`, `inventory_create_draft`, `inventory_create_and_finalize` — IC-1/IC-2/IC-3/IC-7A's own accepted contracts, untouched. `inventory_send_branch_transfer`/`inventory_accept_branch_transfer` call `inventory_finalize_posting_internal` the same same-owner `SECURITY DEFINER` way `inventory_reverse_movement` already does, with `p_explicit_effects=NULL` (catalog effects only) — IC-2's frozen "explicit effects only for type 900" contract is not reopened.
- `inventory_create_reservation`/`inventory_release_reservation` — reused as-is (SECURITY INVOKER, executing with `postgres`'s own privileges when called from within the new SECURITY DEFINER orchestration RPCs, same mechanism already relied on elsewhere in this codebase).
- Reservations/allocations/containers RLS, RepairOrder projection, Zone 5, WZ/201, Phase 10 container relocation — no architecture reopened, per explicit instruction.
- `InventoryEnterpriseService.createBranchTransfer`/`.listBranchTransfers` and their own actions — untouched (create's own status semantics changed at the RPC layer only; the TS wrapper needed no change).
- No UI/component code touched, per explicit instruction.

## Diff summary

**Final, inclusive of the correction pass**: 22 files changed, 2931
insertions(+), 14 deletions(-) against baseline
`97f0e9d923cefbf9043eaefcab25d4c1cf9315b1`. Full unified diff in
`diff.patch` (source files only — the bundle's own files, including
`diff.patch` itself, are excluded from the diff to avoid a self-
referential artifact). `git diff --check` clean on all 22 source files.
