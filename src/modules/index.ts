import { warehouseModule } from "./warehouse/config";
import { teamsModule } from "./teams/config";
import { settingsModule } from "./settings/config";

export const modules = [warehouseModule, teamsModule, settingsModule];

export { warehouseModule, teamsModule, settingsModule };
