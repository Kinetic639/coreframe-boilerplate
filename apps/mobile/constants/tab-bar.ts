/** Visual height of the floating pill tab bar (px). */
export const TAB_BAR_HEIGHT = 64;

/** Gap between the device safe-area bottom edge and the bar's bottom edge (px). */
export const TAB_BAR_SAFE_GAP = 8;

/** Minimum distance from the physical screen bottom when safe-area inset is very small (px). */
export const TAB_BAR_MIN_BOTTOM = 20;
